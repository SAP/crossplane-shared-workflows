# Runs e2e tests.
#
# Approval is handled in the calling job.
#
# Environment variables/secrets that are needed depend on which repo is
# running the workflow. The secrets from the other repo will just be set to empty.
#
# The env vars to be set unfortunately need to be hardcoded in the ENV section of this workflow.
# The only alternative to this would be a central JSON with base64 encoded secrets.
# However, since the secrets are stored in GH environment secrets, which do not allow reading or
# editing, maintaining this is extremely difficult, so this ugly solution was chosen.
#
# For BTP:
# CLI_SERVER_URL contains the CLI server URL
# GLOBAL_ACCOUNT contains the subdomain of the global account
# IDP_URL contains the URL of an IDP that can be connected to the global account
# SECOND_DIRECTORY_ADMIN_EMAIL contains a second email (different from the technical user's email) for the directory
# CIS_CENTRAL_BINDING contents from the service binding of a `cis-central` service
# BTP_TECHNICAL_USER
# TECHNICAL_USER_EMAIL contains the email of the BTP_TECHNICAL_USER
#
# For CF:
# CF_CREDENTIALS
# CF_ENVIRONMENT

#
# More information in the README.md

name: E2E Tests

on:
  workflow_call:
    inputs:
      checkout-ref:
        description: 'the ref for the repo checkout step'
        default: ''
        required: false
        type: string
      skip:
        description: 'skip all jobs (e.g. for doc-only changes); workflow still runs to satisfy branch protection'
        default: false
        required: false
        type: boolean
      concurrency-group:
        description: 'concurrency group for the workflow run (defaults to using the workflow run number, for parallel runs)'
        default: ''
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: ${{ inputs.concurrency-group != '' && inputs.concurrency-group || github.run_id }}
  cancel-in-progress: false

env:
  BUILD_ID: ${{ github.run_number }}-${{ github.run_attempt }} # using shorter run number for BTP character limits
  # hardcoded BTP CLI version
  BTP_CLI_VERSION: 2.116.2
  E2E_BUILD_REGISTRY: index.docker.io/e2e
  CROSSPLANE_CLI_VERSION: v2.0.2
  # renovate: datasource=helm depName=crossplane registryUrl=https://charts.crossplane.io/stable versioning=semver allowedVersions=<2.2.0
  # Pinned to last 2.1.x: 2.2.0+ (crossplane/crossplane#6981) resolves spec.package
  # against a real registry before the cache, breaking the local :latest install path.
  # Hold below 2.2.0 until the digest-pinned side-load fix lands.
  CROSSPLANE_CHART_VERSION: '2.1.8'
  # SHA256 of crossplane-2.1.8.tgz downloaded from charts.crossplane.io/stable
  # Compute with: curl -sL https://charts.crossplane.io/stable/crossplane-<version>.tgz | sha256sum
  CROSSPLANE_CHART_SHA256: '958f331eeab96d4f1c1153c9c0ed36cc91659b79b0779f52e4fc0ffe230863ed'

jobs:
  prepare-matrix:
    if: ${{ !inputs.skip }}
    runs-on: ubuntu-latest
    outputs:
      test-names: ${{ steps.set-matrix.outputs.test-names }}
      # Resolve the checkout ref to a fixed commit SHA once, inside the gated job. Downstream jobs
      # consume this output instead of inputs.checkout-ref, so every checkout provably uses the same
      # commit the maintainer reviewed at approval - no time-of-check/time-of-use gap (CodeQL #67).
      checkout-sha: ${{ steps.pin-sha.outputs.sha }}
    steps:
      - name: checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          #use inputs.checkout-ref to be able to call it from a pull_request_target workflow, since there it needs to be github.event.pull_request.merge_commit_sha and not the default
          ref: ${{ inputs.checkout-ref }}
          # Opt in to checking out fork PR code under pull_request_target. actions/checkout v7 refuses this by default.
# Opt in to checking out fork PR code under pull_request_target. actions/checkout v7 refuses this by default.
# Safe here: the caller's `approve` job (pr-e2e-approval, required_reviewers) must succeed before this workflow's jobs run, so a maintainer inspects the fork diff before it runs with secrets.
          allow-unsafe-pr-checkout: true

      - name: Pin resolved checkout SHA
        id: pin-sha
        # git rev-parse HEAD is the exact commit checked out above. Emitting it as an output freezes
        # a single source of truth for all later checkouts.
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set matrix with test names
        id: set-matrix
        run: |
          echo "test-names=$(find test/e2e -name '*_test.go' | xargs grep -L 'go:build e2e_long' | xargs grep -oh -E 'func (Test[A-Za-z0-9_]+)\(t \*testing\.T\)' | sed 's/func //;s/(t \*testing.T)//' | jq -R . | jq -cs .)" >> $GITHUB_OUTPUT

      - name: Print test names
        run: |
          echo "Test names: ${{ steps.set-matrix.outputs.test-names }}"

  # Pull and verify the Crossplane chart in a dedicated job so that an upstream
  # CDN blip (charts.crossplane.io 403) does not waste the provider build setup
  # and runs in parallel with `prepare-build` for faster wall-clock.
  prepare-helm-chart:
    runs-on: ubuntu-latest
    needs: prepare-matrix
    steps:
      - name: checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Use the SHA pinned by the gated prepare-matrix job, not inputs.checkout-ref, so this
          # checkout provably matches the reviewed commit. See prepare-matrix.checkout-sha.
          ref: ${{ needs.prepare-matrix.outputs.checkout-sha }}
          allow-unsafe-pr-checkout: true

      - name: Install Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: v4.2.4

      - name: Restore Crossplane chart cache
        id: crossplane-chart-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache/charts
          key: crossplane-chart-${{ env.CROSSPLANE_CHART_VERSION }}-${{ runner.os }}

      - name: Pull Crossplane chart (with retry)
        if: steps.crossplane-chart-cache.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          mkdir -p .cache/charts
          retry() {
            local n=1 max=5
            until "$@"; do
              if [ "$n" -ge "$max" ]; then
                echo "::error::'$*' failed after $max attempts"
                return 1
              fi
              echo "attempt $n failed; backing off $((n * 15))s..."
              sleep "$((n * 15))"
              n=$((n + 1))
            done
          }
          retry helm pull crossplane \
            --repo https://charts.crossplane.io/stable \
            --version "${CROSSPLANE_CHART_VERSION}" \
            --destination .cache/charts
          ls -la .cache/charts

      # Defense-in-depth against artifact poisoning (CodeQL alert #64). The `prepare-build`
      # job's `helm pull` step is workflow-mutable; a malicious PR could rewrite
      # it to upload a poisoned chart for matrix legs to install. Pinning the
      # expected SHA256 (sourced from upstream charts.crossplane.io/stable
      # index.yaml's `digest:` field) means a poisoned tarball fails the build
      # before reaching matrix legs. Update the constant on every Crossplane
      # version bump.
      - name: Verify Crossplane chart integrity
        run: |
          set -euo pipefail
          cd .cache/charts
          EXPECTED="${CROSSPLANE_CHART_SHA256}"
          ACTUAL="$(sha256sum "crossplane-${CROSSPLANE_CHART_VERSION}.tgz" | awk '{print $1}')"
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "::error::Crossplane chart SHA256 mismatch: expected $EXPECTED, got $ACTUAL"
            exit 1
          fi
          echo "Crossplane chart SHA256 verified: $ACTUAL"

      - name: Save Crossplane chart cache
        if: success() && steps.crossplane-chart-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache/charts
          key: ${{ steps.crossplane-chart-cache.outputs.cache-primary-key }}

      - name: Upload Crossplane chart
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: crossplane-chart
          path: .cache/charts
          retention-days: 7
          overwrite: true
          if-no-files-found: error

  # Build image + xpkg + CLI tools once; the matrix consumes them as artifacts.
  # NOTE: after the artifact retention window expires (retention-days below),
  # "Re-run failed jobs" fails — prepare-build won't re-run, so its artifacts are gone.
  # Use "Re-run all jobs" to rebuild them.
  prepare-build:
    runs-on: ubuntu-latest
    needs: prepare-matrix
    steps:
      - name: checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Use the SHA pinned by the gated prepare-matrix job. See prepare-matrix.checkout-sha.
          ref: ${{ needs.prepare-matrix.outputs.checkout-sha }}
          submodules: true
          allow-unsafe-pr-checkout: true

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: Restore CLI tools cache (kind, kubectl, crank, btp)
        id: cli-tools-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache/tools
          key: e2e-tools-${{ runner.os }}-${{ runner.arch }}-crank-${{ env.CROSSPLANE_CLI_VERSION }}-btp-${{ env.BTP_CLI_VERSION }}-${{ hashFiles('build/makelib/k8s_tools.mk') }}

      - name: Fetch CLI tools once (kind, kubectl, crank, btp)
        if: steps.cli-tools-cache.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          retry() {
            local n=1 max=5
            until "$@"; do
              if [ "$n" -ge "$max" ]; then
                echo "::error::'$*' failed after $max attempts"
                return 1
              fi
              echo "attempt $n failed; backing off $((n * 15))s..."
              sleep "$((n * 15))"
              n=$((n + 1))
            done
          }
          vars="$(make --no-print-directory build.vars | grep -E '^(KIND|KUBECTL)=')"
          eval "$vars"
          : "${KIND:?}" "${KUBECTL:?}" "${CROSSPLANE_CLI_VERSION:?}" "${BTP_CLI_VERSION:?}"
          CRANK=".cache/tools/linux_x86_64/crossplane-cli-${CROSSPLANE_CLI_VERSION}"
          BTP=".cache/tools/linux_x86_64/btp-${BTP_CLI_VERSION}"
          retry make "$KIND" "$KUBECTL"
          echo "Installing crossplane cli version ${CROSSPLANE_CLI_VERSION}"
          retry curl -fsSL --create-dirs -o "$CRANK" "https://releases.crossplane.io/stable/${CROSSPLANE_CLI_VERSION}/bin/linux_amd64/crank"
          chmod +x "$CRANK"
          echo "Installing BTP CLI version ${BTP_CLI_VERSION}"
          retry curl -fsSL \
            --cookie "eula_3_2_agreed=tools.hana.ondemand.com/developer-license-3_2.txt" \
            -o "${BTP}.tar.gz" \
            "https://tools.hana.ondemand.com/additional/btp-cli-linux-amd64-${BTP_CLI_VERSION}.tar.gz"
          tar -xzf "${BTP}.tar.gz" -O linux-amd64/btp > "$BTP"
          chmod +x "$BTP"

      - name: Save CLI tools cache (kind, kubectl, crank, btp)
        if: success() && steps.cli-tools-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .cache/tools
          key: ${{ steps.cli-tools-cache.outputs.cache-primary-key }}

      - name: Set provider image name
        run: |
          repo="${GITHUB_REPOSITORY##*/}"
          echo "E2E_PROVIDER_IMAGE=${{ env.E2E_BUILD_REGISTRY }}/${repo#crossplane-}-amd64" >> "$GITHUB_ENV"

      - name: Build provider image
        run: make build BUILD_REGISTRY=${{ env.E2E_BUILD_REGISTRY }} PLATFORMS=linux_amd64

      - name: Export provider image
        run: docker save "${{ env.E2E_PROVIDER_IMAGE }}" -o provider-image.tar

      - name: Upload provider image
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provider-image
          path: provider-image.tar
          retention-days: 7
          overwrite: true
          if-no-files-found: error

      - name: Upload provider xpkg
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: provider-xpkg
          path: _output/xpkg
          retention-days: 7
          overwrite: true
          if-no-files-found: error

      - name: Upload CLI tools
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-tools
          path: .cache/tools
          retention-days: 7
          overwrite: true
          if-no-files-found: error

  e2e-test:
    runs-on: ubuntu-latest
    needs: [prepare-matrix, prepare-helm-chart, prepare-build] # prepare-helm-chart provides crossplane-chart artifact; prepare-build provides image/xpkg/tools artifacts
    environment:
      name: pr-e2e-no-approval  # approval is on the preceding approval job
      deployment: false
    strategy:
      fail-fast: false
      matrix:
        testName: ${{ fromJson(needs.prepare-matrix.outputs.test-names) }}
    steps:
      - name: checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Use the SHA pinned by the gated prepare-matrix job, not inputs.checkout-ref. This job
          # holds the secrets; pinning to the reviewed commit closes any time-of-check/time-of-use
          # gap (CodeQL #67). See prepare-matrix.checkout-sha.
          ref: ${{ needs.prepare-matrix.outputs.checkout-sha }}
          submodules: true
          allow-unsafe-pr-checkout: true

      - name: Assert checked-out commit matches the reviewed SHA
        # Belt-and-suspenders: fail before any secret is used if the checkout ever differs from the
        # commit prepare-matrix pinned at approval.
        run: |
          expected='${{ needs.prepare-matrix.outputs.checkout-sha }}'
          actual="$(git rev-parse HEAD)"
          if [ "$actual" != "$expected" ]; then
            echo "::error::checked-out $actual != approved $expected"; exit 1
          fi

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
      - run: go version

      - name: Download CLI tools
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: e2e-tools
          path: .cache/tools

      - name: Restore tool executable bits
        # upload-artifact drops the +x bit
        run: find .cache/tools -type f -exec chmod +x {} +

      - name: Download provider xpkg
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: provider-xpkg
          path: _output/xpkg

      - name: Download provider image
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: provider-image
          path: ${{ runner.temp }}/artifacts/provider-image

      - name: Validate provider image artifact
        run: test -f "${{ runner.temp }}/artifacts/provider-image/provider-image.tar"

      - name: Route Docker Hub pulls through mirror.gcr.io
        # Routing docker.io through Google's pull-through cache for kindest/node.
        run: |
          set -euo pipefail
          sudo mkdir -p /etc/docker
          if sudo test -s /etc/docker/daemon.json; then
            sudo jq '. + {"registry-mirrors":["https://mirror.gcr.io"]}' /etc/docker/daemon.json \
              | sudo tee /etc/docker/daemon.json.tmp >/dev/null
            sudo mv /etc/docker/daemon.json.tmp /etc/docker/daemon.json
          else
            printf '%s\n' '{"registry-mirrors":["https://mirror.gcr.io"]}' | sudo tee /etc/docker/daemon.json >/dev/null
          fi
          sudo systemctl reload docker
          for _ in $(seq 1 10); do
            case "$(docker info --format '{{.RegistryConfig.Mirrors}}')" in
              *mirror.gcr.io*) echo "registry mirror applied"; exit 0 ;;
            esac
            sleep 1
          done
          echo "::error::mirror.gcr.io not applied to docker daemon"; exit 1

      - name: Load provider image
        run: docker load -i "${{ runner.temp }}/artifacts/provider-image/provider-image.tar"

      - name: Install Helm
        uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1
        with:
          version: v4.2.4

      - name: Download Crossplane chart
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: crossplane-chart
          path: .cache/charts

      - name: Resolve Crossplane chart path
        run: |
          set -euo pipefail
          CHART="$(ls .cache/charts/crossplane-*.tgz | head -n 1)"
          test -f "$CHART" || { echo "::error::no chart at .cache/charts/crossplane-*.tgz"; exit 1; }
          echo "CROSSPLANE_CHART_PATH=$(realpath "$CHART")" >> $GITHUB_ENV

      - name: Install BTP CLI from tools artifact
        run: sudo cp ".cache/tools/linux_x86_64/btp-${BTP_CLI_VERSION}" /usr/local/bin/btp

      - name: Run e2e test
        timeout-minutes: 120
        run: |
          set -uo pipefail
          for attempt in 1 2 3; do
            rm -f test-output.log
            make test-acceptance ACCEPTANCE_DEPLOY=local-deploy-prebuilt BUILD_REGISTRY=${{ env.E2E_BUILD_REGISTRY }} testFilter=${{ matrix.testName }} && exit 0
            rc=$?
            grep -qE 'crossplane helm chart repo|upgrade helm repo|install crossplane Helm chart|not a valid chart repository or cannot be reached|failed to pull image' test-output.log 2>/dev/null || exit "$rc"
            echo "::warning::transient setup flake (helm/node-image); retry $attempt/3"
            sleep $((attempt * 15))
          done
          exit "$rc"
        env:
          # secrets list for BTP
          CIS_CENTRAL_BINDING: ${{ secrets.CIS_CENTRAL_BINDING }}
          BTP_TECHNICAL_USER: ${{ secrets.BTP_TECHNICAL_USER }}
          CLI_SERVER_URL: ${{ secrets.CLI_SERVER_URL }}
          GLOBAL_ACCOUNT: ${{ secrets.GLOBAL_ACCOUNT }}
          TECHNICAL_USER_EMAIL: ${{ secrets.TECHNICAL_USER_EMAIL }}
          SECOND_DIRECTORY_ADMIN_EMAIL: ${{ secrets.SECOND_DIRECTORY_ADMIN_EMAIL }}
          IDP_URL: ${{ secrets.IDP_URL }}
          # secrets list for CF
          CF_CREDENTIALS: ${{ secrets.CF_CREDENTIALS }}
          CF_ENVIRONMENT: ${{ secrets.CF_ENVIRONMENT }}

      - name: Upload test logs
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-logs-${{ matrix.testName }}-attempt-${{ github.run_attempt }}
          path: |
            test-output.log
            test/e2e/logs/post-tests/
          retention-days: 14
          if-no-files-found: warn

  cleanup-e2e:
    runs-on: ubuntu-latest
    needs: [prepare-matrix, prepare-build, e2e-test]
    environment:
      name: pr-e2e-no-approval
      deployment: false
    if: always() && !inputs.skip && needs.prepare-matrix.result == 'success' && needs.prepare-build.result == 'success' # intentional: workflow cancellation skips cleanup; resources will persist until the next run
    steps:
      - name: checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # use github.base_ref to checkout the base branch, since the cleanup should always run on the base branch to make it harder for malicious code execution. Consequently, this will not pick up changes to the cleanup.go file in the PR, but that is accebtable.
          ref: ${{ github.base_ref }}
          submodules: true

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod

      - name: Download CLI tools
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: e2e-tools
          path: .cache/tools

      - name: Restore tool executable bits
        # upload-artifact drops the +x bit
        run: find .cache/tools -type f -exec chmod +x {} +

      - name: Install BTP CLI from tools artifact
        run: sudo cp ".cache/tools/linux_x86_64/btp-${BTP_CLI_VERSION}" /usr/local/bin/btp

      - name: clean up cluster
        run: |
          go run .github/workflows/cleanup.go
        env:
          # secrets list for BTP
          CIS_CENTRAL_BINDING: ${{ secrets.CIS_CENTRAL_BINDING }}
          BTP_TECHNICAL_USER: ${{ secrets.BTP_TECHNICAL_USER }}
          CLI_SERVER_URL: ${{ secrets.CLI_SERVER_URL }}
          GLOBAL_ACCOUNT: ${{ secrets.GLOBAL_ACCOUNT }}
          # secrets list for CF
          CF_CREDENTIALS: ${{ secrets.CF_CREDENTIALS }}
          CF_ENVIRONMENT: ${{ secrets.CF_ENVIRONMENT }}

  # required for test matrix status reporting. Ignores cleanup-e2e job failure/success.
  e2e-status:
    runs-on: ubuntu-latest
    needs:
      - prepare-matrix
      - prepare-helm-chart
      - prepare-build
      - e2e-test
    timeout-minutes: 1
    if: always() && !inputs.skip
    steps:
      - name: Fail for failed prepare-matrix job
        if: |
          needs.prepare-matrix.result == 'failure' ||
          needs.prepare-matrix.result == 'cancelled'
        run: exit 1
      - name: Fail for failed prepare-helm-chart job
        if: |
          needs.prepare-helm-chart.result == 'failure' ||
          needs.prepare-helm-chart.result == 'cancelled'
        run: exit 1
      - name: Fail for failed prepare-build job
        if: |
          needs.prepare-build.result == 'failure' ||
          needs.prepare-build.result == 'cancelled'
        run: exit 1
      - name: Fail for failed matrix jobs
        if: |
          needs.e2e-test.result == 'failure' ||
          needs.e2e-test.result == 'cancelled'
        run: exit 1
